NODE_ENV=development
PORT=3001

DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=market_os
# Set DB_SSL=true when pointing at a hosted Postgres that requires TLS.
DB_SSL=false

JWT_SECRET=change-this-in-production
JWT_EXPIRES_IN=7d
