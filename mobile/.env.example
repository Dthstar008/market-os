# API base URL. "localhost" only works in the web preview or an iOS simulator.
# On a physical device or Android emulator, use your machine's LAN IP, e.g.:
# EXPO_PUBLIC_API_URL=http://192.168.1.50:3001
EXPO_PUBLIC_API_URL=http://localhost:3001
